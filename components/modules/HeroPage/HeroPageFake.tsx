import { createShimmer } from "@/utils/shimer";
import "./style.css";

export const HeroPageFake = () => {
  return (
    <div
      className={`relative h-[500px] md:rounded-b-[100px] rounded-b-[50px] overflow-hidden bg-black`}
    ></div>
  );
};
